{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Редактирование метки</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('point') }}">
            Обратно к списку
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
